# No Excuses — the gate, one line per section

**An index, not a history.** Which section a check lives in, the feedback lines that section stands for, and the name `--only`
takes. The paragraphs builds 14–46 appended here are in `../docs/GATE-HISTORY.md`, untouched and frozen. The rules for adding a
check are in `../CLAUDE.md` → The gate.

- **The gate:** `npm test` with no flags, once, before the push. It prints each failure, one line per section and the verdict.
- **The fix loop:** `npm test -- --only <section> --bail`. `--from 44` runs build 44 and everything after it, `--verbose` prints
  every pass line, `--labels <file>` writes every check by section. A partial run says PARTIAL RUN and never stands in for the gate.
- **A new section** adds one row here. A check added to an existing section changes that row only if the section now stands for a
  feedback line it did not before.
- **The verdict always prints (build 55, v29 item 17).** A thrown error is a named FAILURE, not a dead process: the summary, the
  failure list and the exit code all still happen. The build-54 review's first full run died on a detached frame and printed nothing.
- **`../_review/` is optional (build 55, v29 item 17).** Ten sections read the review pipeline, which lives outside the site tree.
  Each is skipped BY NAME when it is not there (`... — SKIPPED: ../_review is not in this checkout`), and its section keeps every
  other check it has, so `npm test` passes on a clone of `site` alone — which is what Codemagic gets.

| `--only` | Section, as printed | Stands for |
|---|---|---|
| `static` | static checks | A6 one build number, now `index.html` ×2 (v18 S.2 `v0.N`; **v29 item 7, build 55** — the third copy went with the inline script), **v29 items 7 / 15 / 16 (build 55)** the S4 CSP meta and no inline script, S7's https / native gates on the poll, A8's one `haptic()` and no `navigator.vibrate` in `games/` or `run/`, one name per achievement across `ACH` and `KEY_ROSTER`; A2 `config/` is data, A3 engine imports, A4 no screen imports a screen; v16 §1 / 1.6 / §5 (music data, the unlock sound, one-line intro) with v17 B.30; L5's Reaction budgets (v14 C.1–C.4, v18 B.1c / B.3, v19 C.5 / C.6, v24 F.1) |
| `cold start` | cold start (empty storage) | L1 the title sequence plays on a new profile; v14 1.2 NO EXCUSES is one node that never moves |
| `locked decisions` | locked decisions (fresh profile) | L1, L2, L3, L7, L9 on a fresh profile — runs `cold start` with it |
| `pick sheets` | pick sheets (all unlocked) | every game's pick sheet and every side screen opens (v23 L.4a `s-custom`); **v28 items 7 and 14 (build 53)** — every Quick Tap and Dots mode shows its first target during the countdown and it is the one the run starts on, and the mode picker is a bottom sheet pinned to the screen with the map dimmed behind it and the tapped tile scrolled clear above it |
| `sheet copy` | sheet copy comes from SET_COPY (L5) | L5 / v14 §5 every Set and Streak line on every sheet comes from `SET_COPY` |
| `one set run` | one Set run and one Streak run per game (first mode) | a Set and a Streak of every game driven to its result; v14 6.3 / 6.18, v15 3.9, v16 A.3, v18 B.3d |
| `pass & play` | pass & play Quick Tap | L3 pass & play: two whole runs with the hand-over screen between |
| `two-player` | two-player (v15 section 4) | v15 §4.1–4.6 turn-taking inside one run, Sequence and Find versus; A.3 / L10 nothing stored |
| `storage` | storage fixtures | **v29 items 5 / 6 / 16 (build 55)** an inherited `Object.prototype` name in `runs[].g` and in `prefs.lastGame` / `scale` / `bg` boots clean and is repaired, a non-finite or negative number in a run is rejected outright, `ach` / `unlock` / `bars` are capped, a board cell is escaped, and `hd_s` reads any Set; A5 the one key `ne` and its ladder to v6 (v18 B.2 / B.4, v19 C.5, v21 F.4, v23 L.10 / L.7c), corrupt records, the 600-run cap; v26 item 3 `menuOpened` and the retired `devMeter` (build 48) |
| `challenge` | challenge links | S1 a hostile score lands as text; S2 a bad link is no challenge and a link's run is never on a board |
| `side screens` | side screens (v14 section 8) | v14 8.1–8.10 (Customise as v23 L.4a / L.11a left it); **v28 items 1, 4 and 6 (build 53)** — R3, no entry animation on any Progress tab or filter; Secret last in every filter and drawn like a locked ordinary row; one "N of M unlocked" line per tab with Secret out of M until one is found; every Customise-unlock row shows the thing it unlocks |
| `key` | the key (v14 section 9) | v14 §9 and C.5–C.7 — contributors and count from the config, a bar per combination, a bar clears once and solo only; the meter line (A.6 / v23 L.8a) |
| `chain` | the chain and its screens (v15 sections 1 and 2) | v15 §1.0–1.5 and §2.1–2.5 with L6 — thresholds either side of the line, one requirement builder, a mid-run earn survives a quit |
| `runs` | the runs (v15 section 3) | **v29 items 2 / 3 / 4 / 9 / 14 (build 55)** quitting during round 1 banks nothing, a countdown tap after an aborted round-based run is dropped, the phone going to sleep ends the run and banks nothing, a screen change out of a live run ends it, and the HUD's rolling figure is held to its bar's ceiling; v15 §3.1–3.12 with L4 / L5; v26 item 1 the Go / No-go Set HUD (build 48); v26 §B2 / A9 the shape difficulty standard, every game's deal and the 45° Hidden wall (build 50) |
| `keys` | the keys, the surface and #375 (v15 sections 5 and 6) | **v29 items 1 / 10 / 11 / 13 (build 55)** the meter is 0–300 (key 1 + Pro renders 200%), a dev-opened tier is shown and never banked, the Next card only offers something earnable now, and a clip that will not load says so; v15 §5.1–5.4 and §6.1–6.5; #375a–c two-player defects; v26 items 3, 9, 10, 11, 12 — the green menu items (earned through play), the key cards and count line, all three key reveals to their last frame, the key asking before its chest opens (Aiden's answer after build 48), the version label on the home menu only (build 48); v26 items 2 and 4 (build 49) — the map's first open drawn out to 6–8s in its order, once, replayed by Fresh game; an unwatched clip pulses on About; **v27 item 14 (build 51)** — one earned animation per key, two seconds at most, at least three quarters of it movement, a tap skips it and the chest prompt waits for it; **v28 item 15 (build 53)** — the earn MUSIC is the clock: each tier's animation is as long as its own earn music to within a beat, the music starts on the first frame rather than the closing flash, a `rise` and `land` finale fills the back half and the assembly is still at least a quarter of it; **v29 item 3 (build 54)** — Aiden shortened two of the three: Pro 4.10s → 3.00s and Author 6.70s → 4.00s, cut at the track’s tail with the assemblies untouched, so what shrank is the `rise`; **v27 items 7, 8, 9, 10 and 11 (build 52)** — Aiden's new eight messages and their four kinds of lock, a Gauntlet's row not in the list at all until its chest opens while the counter still says "of 8", and the one shared video player: 16:9 inset from every edge, the unlocking chest's colour in its glow, captions below a hidden track, tap outside to close, and a power-on and power-off built into the player |
| `button actions` | button actions (every data-act at least once) | every `data-act` control driven at least once |
| `chests` | chests | v26 items 5, 6, 8, 12 and 13 (build 49) — all four chests' rewards fly out of the lid one at a time and settle under the chest, in colour, the About video among them, the name and "tap to continue" after the last lands, the card below them; the two Gauntlet tiles and their placeholder screen. **v27 items 2, 4 and 13 (build 51)** — a Gauntlet is nothing at all until its chest opens (no tile, no lock, no connector, no cell, no beat in the map's first open) and arrives on that chest's spill; the four names are Games / Skill / Pro / Author from one source in `config/`, spelled nowhere else; each chest matches the key that opens it (the gold chest moved to Skill, the Pro chest redrawn from the Pro key). v26 items 7 and 12 (build 48) — one saved value: the Games chest and key 1 EARNED BY RUNS and reached by Testing's buttons land in the same state, and after every step the store, the map, the Keys screen and the menu agree on a state play can reach; the 0–300 meter, the Pro chest at 200, locked chests in words, no percentage on the Games chest. **v28 items 12, 13, 16 and 17 (build 53)** — a chest opened by a key UNLOCKS and never breaks (Skill, Pro and Author all run assemble · turn · lid · spill in their own key's colour and glyph, no coloured swatches left anywhere), the breaking is the Games chest's alone at one crack per game finished with the seventh bursting it, and the congratulations screen stages its blocks with Continue last, throws that chest's own confetti and plays its own celebration sound, with the message drawn as the powered-off video player; **v29 item 5 (build 54)** — the Author chest wears its own language again over that one mechanism: the four turn steps still come first and in order on all three, and `black` / `spikes` / `split` / `widen` / `recede` are the Author chest’s alone |
| `music` | music | v26 §B1 (build 49) — the four result sounds climb and a round's is one note shorter with bass; End of run lands before the tier plays; seven whoosh versions at random and longer title whooshes; Sigh held; the Skill chest from key 1's theme, the Pro chest and Thorn earned bigger; **v27 items 1, 5, 6 and 14 (build 51)** — each title line is one impact on its own frame, the Games chest's seven squares tick a step higher each, its rewards pop clear of its own chord (and the Pro chest's are untouched), and every named step of a key's earned animation has its own sound; **v27 items 10 and 14 (build 52)** — the video player's power-on and power-off are one soft pair for every clip, and the three key earn sounds climb further (Pro "even more epic", Author "epic super duper") with every note still inside the key-theme rule; **v29 items 8 / 12 (build 55)** a tap-to-skip silences the earn music (the handle `Snd.keyEarn` hands back), and `AC()` no longer resumes by itself — every resume goes through `revive()` (F.2). |
| `build 27` | build 27 — v16 | v16 (batch 12) — the music engine, the Timing unlock, Find versus, the intro, A.3 |
| `build 28` | build 28 - v17 sections B.1 to B.18 | v17 §B.1–B.18 — the chain and the scoring |
| `build 29` | build 29 - v17 sections B.19 to B.26 | v17 §B.19–B.26 — the front of the app |
| `build 30` | build 30 - v17 sections B.27 to B.33 | v17 §B.27–B.33 — music and sound |
| `build 31` | build 31 - v18 sections B.1 to B.14 | v18 §B.1–B.14 — the runs |
| `build 32` | build 32 - v19 section C and v18 sections B.15 to B.27 | v19 §C.1–C.6; v18 §B.15–B.27 — Go / No-go's deal and score, the three tiers, the chests |
| `build 33` | build 33 - v18 sections B.28 to B.32 | v18 §B.28–B.32 — the Music row, `cut()`, locked lines, self-hosted fonts |
| `build 35` | build 35 - batch 15, bugs and the runs | v21 §F.1–F.5, §G.7; v20 §D.1–D.3, §D.8–D.10; #415; the Verdict Desk |
| `build 36` | build 36 - the frozen clock and the verdict export | v22 §J.1; the Verdict Desk export (version 658) |
| `build 37` | build 37 - keys and chests | v21 §G.1–G.4, §G.8; v20 §D.4, §D.7; v22 §K; Aiden's 2026-09-14 data fixes (#414) |
| `build 38` | build 38 - the tile keeps its amber, Author waits for the Pro chest | Aiden's two answers to build 37 — v22 §K's tile, Author waits for the Pro chest. `--only "build 38"` runs both parts |
| `build 38` | build 38 - #426 Pro and Author placeholders | #426 (A.2 amended) placeholders, NEVER OVERWRITE, retro credit on arrival; v24 §E |
| `build 39` | build 39 - batch 16, the surface | v23 §L.2–L.5 — Customise out of Progress, the partition, the labels, the stamp |
| `build 40` | build 40 - batch 16, four chests and the meter | v23 §L.8 a–c f, §L.10 a–c e, §L.11 a c, §L.12, §M.2; v21 G.8 extended |
| `build 41` | build 41 - batch 16, the moments | v23 §L.6, §L.8 d–e, §L.9 a–d, §L.10 d, §L.11 b d e; **v29 item 5 (build 54)** — the Author chest’s nine named steps, the four shared then its own five |
| `build 42` | build 42 - batch 16, the key themes | v23 §L.7 a–e; **v28 items 2 and 3 (build 53)** — the Everywhere row is gone and Customise has ONE Music row holding the three key tracks, each locked until its KEY is earned (not until the chest that key opens), titled by its theme and never by a key's name, and whatever is picked plays in the menu too; **v29 item 4 (build 54)** — ONE RULE FOR BOTH: a game's track picked in that row is the menu's music as well, not just a key theme, while a game screen still plays its own |
| `build 43` | build 43 - batch 17, chests and keys | v24 §A, §B.1–B.3, §B.5, §C |
| `build 44` | build 44 - batch 17, the key roster, Aiden's bars, the goal and six game tweaks | v24 §D, §E, §F.1, §F.3–F.7 |
| `build 45` | build 45 - batch 18, fixes, state and the catalogue | v25 items 3, 4, 5, 8, 9, 10, 12, 14, 16–21 |
| `build 46` | build 46 - batch 18, the unlock experience, sound and About | v25 items 6, 7, 11, 13, 15, 22, 1, 2, 23; **v29 item 3 (build 54)** — the key-earn skip waits `EARN_SKIP_AT` (1.5s): a tap inside the window does nothing and the ceremony plays on, a tap after it jumps to the finished state |
